services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    environment:
      DATABASE_URL: sqlite:////data/xunlei.db
      XUNLEI_MEDIA_ROOT: /data/media
      XUNLEI_MAX_UPLOAD_BYTES: ${XUNLEI_MAX_UPLOAD_BYTES:-314572800}
      MINIMAX_API_KEY: ${MINIMAX_API_KEY:-}
      MINIMAX_MODEL: ${MINIMAX_MODEL:-MiniMax-M3}
      MINIMAX_BASE_URL: ${MINIMAX_BASE_URL:-https://api.minimaxi.com}
      XUNLEI_PRELOAD_ASR: ${XUNLEI_PRELOAD_ASR:-false}
      XUNLEI_PUBLIC_BASE_URL: ${XUNLEI_PUBLIC_BASE_URL:-}
      VOLC_ASR_API_KEY: ${VOLC_ASR_API_KEY:-}
      VOLC_ASR_APP_ID: ${VOLC_ASR_APP_ID:-}
      VOLC_ASR_ACCESS_TOKEN: ${VOLC_ASR_ACCESS_TOKEN:-}
      VOLC_ASR_RESOURCE_ID: ${VOLC_ASR_RESOURCE_ID:-volc.seedasr.auc}
      VOLC_ASR_BASE_URL: ${VOLC_ASR_BASE_URL:-https://openspeech.bytedance.com/api/v3/auc/bigmodel}
      VOLC_ASR_POLL_INTERVAL_SECONDS: ${VOLC_ASR_POLL_INTERVAL_SECONDS:-2}
      VOLC_ASR_TIMEOUT_SECONDS: ${VOLC_ASR_TIMEOUT_SECONDS:-600}
      XUNLEI_PRECISE_MODEL_PATH: /models/faster-whisper-large-v3
      HF_HOME: /models/huggingface
      MODELSCOPE_CACHE: /models/modelscope
    volumes:
      - xunlei-data:/data
      - xunlei-models:/models
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/v1/health', timeout=5)",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "${XUNLEI_WEB_PORT:-8080}:80"

volumes:
  xunlei-data:
  xunlei-models:
